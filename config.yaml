# fing 默认配置（无环境变量时用）
# 推荐用 config.development.yaml 或 config.docker.yaml

mode: dev
secret: dev-secret-please-change-in-production-min-32-chars
level: 4
port: 9765

dataSource:
  main: fing:fingpass@tcp(localhost:3306)/fing?charset=utf8mb4&parseTime=True&loc=Local

redis:
  addr: localhost:6379

es:
  esUrl: ""
  esUsername: ""
  esPassword: ""

email:
  host: smtp.gmail.com
  name: "fing"
  email: noreply@example.com
  password: ""